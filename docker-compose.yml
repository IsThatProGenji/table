version: '3.8'

services:
  app:
    build:
      context: .
    container_name: farcraft-table
    ports:
      - '127.0.0.1:3002:3000'
    restart: always
    environment:
      - NODE_ENV=production
    command: ['pm2-runtime', 'start', 'yarn', '--name', 'app', '--', 'start']
